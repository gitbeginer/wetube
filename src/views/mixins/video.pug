mixin video(info)
    div
        h4
            a(href=`/videos/${info.id}`)=info.title
        p=info.description
        ul 
            each word in info.hashtags
                li=word
        small=info.createAt
        hr 